[project]
name = "simple-text-mcp-http-server"
version = "0.1.0"
description = "Simple MCP HTTP Server"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "mcp"
]
